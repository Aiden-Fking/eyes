server:
  port: 8080

spring:
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/eyes}
    username: ${SPRING_DATASOURCE_USERNAME:eyes}
    password: ${SPRING_DATASOURCE_PASSWORD:eyes}
